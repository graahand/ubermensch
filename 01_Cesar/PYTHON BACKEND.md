
**Stack 3: Python Backend**

- Python 3.12, Poetry (or uv) for dependencies

- Framework: FastAPI + Uvicorn (or Flask)

- Validation/DTO: Pydantic

- DB (Optional): SQLAlchemy + Alembic; SQLite/PostgreSQL

- Testing: pytest; HTTP: httpx

- Quality: Ruff (lint) + Black (format) + mypy (types) + pre-commit

- Tools: Makefile/Taskfile; Docker